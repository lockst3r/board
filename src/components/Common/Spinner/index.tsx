/* import { CircularProgress, withStyles, LinearProgress } from '@material-ui/core'
import * as React from 'react'
export const Spinner = withStyles((theme) => ({
  root: {
    position: 'absolute',
    //left: 'calc(50% - 20px)',
    //top: 'calc(50% - 20px)',
    width: '100%',
    zIndex: 100,
  },
}))(LinearProgress) */
